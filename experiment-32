def simulate_dos(target, requests):
    print(f"Simulating DoS on {target}")
    for i in range(requests):
        print(f"Sending request {i+1} to {target}")
    print("Target Overloaded!")

simulate_dos("server.com", 5)
